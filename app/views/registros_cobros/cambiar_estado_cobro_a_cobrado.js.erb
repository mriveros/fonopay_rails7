<% if @actualizado %>
  
  $("#form_buscar_cobros_consultas_id").val('<%= @cita.id %>');
  $("#form_buscar_cobros_consultas[estado_cobro_id]").val('<%= PARAMETRO[:estado_cobro_cobrado]%>');
  $("#form-buscar-pagos-cobros-detalles").submit();

  setTimeout(function(){ 

    swal({title: "Atención", text: "<%= j(@msg)%>", html: false})
    
  }, 1000);

<% else %>

    swal({title: "Atención", text: "<%= j(@msg)%>", html: false})

<% end %>